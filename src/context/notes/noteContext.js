import { createContext } from "react";

const noteContext = createContext(); //Creating a context. A context is something that is used to hold state, and in this case it will hold state related to note

export default noteContext; 